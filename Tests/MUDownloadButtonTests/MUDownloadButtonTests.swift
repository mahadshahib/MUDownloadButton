import XCTest
@testable import MUDownloadButton

final class MUDownloadButtonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MUDownloadButton().text, "Hello, World!")
    }
}
